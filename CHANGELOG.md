## v2.0.1.0 — 加载顺序体系完善与导入导出

### 新增

- **「应用顺序」= 观测顺序 → 加载顺序**：点击后把观测视图的排列（`displayOrder` 线性化，分组按序展开）整体写为目标方案/集合的 `loadOrder`（此前只是给启用 Mod 赋递增序号，实际不生效）
- **方案 / 集合下拉框内「新建 / 导出 / 导入」三按钮（同行）**：
  - 方案：导出（读取方案 JSON，含备注/分类时弹隐私提示）、导入（校验名称/enabledMods/版本≤2，v1 自动迁移、缺失 `loadOrder` 自动派生，同名覆盖确认）
  - 集合：导出（`exportJson` → JSON）、导入（`importJson` 规范化，自动切换）
  - 导入后**直接切换到对应的方案/集合**
  - 操作后下拉框自动收起
- **导入缺失 Mod 检测**：方案/集合导入后 `detectMissingMods` 弹窗列出工坊/本地缺失项
- **禁用 Mod 自动归到加载末尾**：切换启用状态时对 `loadOrder` 做稳定分区——已启用在前（相对顺序不变）、禁用移到末尾；启用则自然上移
- **加载顺序改为 0 基**：方案/集合观测卡片、加载列表序号从 0 开始（输入 N = 移动到第 N 位）；写游戏的 ModOrder 也改为 **0..N-1**，与游戏内模组管理的 0 基一致

### 改进

- **方案导入名称做文件名非法字符过滤**并弹窗确认（不再因 `/ \ : * ? " < > |` 导致保存失败）；方案页"新建方案"、集合建方案同步统一
- **批量启用/禁用走受控 toggle**：修复方案/集合页多选右键"批量禁用"失效（此前误写全局 store，容器 `enabledMods` 未变）
- 方案页添加（面板）、合并集合、移出成员后**刷新下拉框 mod 计数**（集合页为响应式 store 自动更新）
- 移除集合页头部独立"新建集合"按钮（保留下拉框内"新建"）
- 清理未用组件：`ProfileManager`、`SchemeSelector`、`CollectionPanel`、`ModContextMenu`（`MissingModsDialog` 保留）
- 撤回"隐藏空分组"：分组保留作**成员缺失提示**，不再随保存删改方案结构

### 修复

- 受控 `setGroups`/`setDisplayOrder` 透传函数式更新，修复多选拖入分组只移动一个（方案页与集合页）
- `useCardDrag` 拖拽闭包选择状态过期导致 `multiDrag` 恒 false 且误清多选
- 方案/集合编辑不再误置 `isDirty`，关闭启动器不再误报"未保存的更改"；启动时若存在激活方案仍按最新编辑同步写盘
- 集合分组持久化到集合数据，切换视图/页面重挂载不再丢失
- 标签下拉面板 portal 到 `document.body`，不再被 `overflow` 裁剪且自动翻转；避免点击被面板拦截而误关标签窗口

## v2.0.0.0 — 子页架构与加载顺序体系

### 新增

- **子页架构**：左侧栏 6 页导航（启动 / 方案 / 集合 / 已读取 Mod / 设置 / 日志），独立子页与切换
- **方案页观测/加载双模式**：分组浏览（观测）与加载顺序（加载）一键切换；新增独立 `loadOrder` 字段，浏览组织与加载序列彻底解耦（立绘分组里的 Mod 也能排在框架之前加载）
- **集合页观测/加载双模式**：与方案页同构；集合新增 `loadOrder` 字段，加载顺序独立可调
- **激活方案按加载序生成全量稠密 ModOrder（0..N-1，与游戏内 0 基一致）**：与游戏自身的全量重排对齐，相对加载顺序不再被归一化掉
- **行内序号输入框**：输入 N = 将该 Mod 直接移动到第 N 位，原位置及下方自动 +1；观测卡片与加载列表同一套交互
- **集合建方案命名对话框**：从集合创建方案时弹出命名窗口，命名后跳转方案页并自动选中新方案
- **右键"移出方案/移出集合"**：单选与多选菜单均支持，选中项批量移出并即时落盘
- **方案名非法字符弹窗确认**：名称含 `\ / : * ? " < > |` 时弹出清理结果提示，由用户选择继续或取消
- **标签管理窗口**：搜索 + 单击单选 / Ctrl / Shift 多选 + 行尾箭头修改（名称、颜色，重名检测）+ 底部新建；原生颜色选择器
- **状态栏显示应用消息**：此前 `lastMessage` 从未渲染（所有提示不可见），现统一显示在底部状态栏

### 改进

- 方案/集合的 mod 卡片不再随启用/禁用状态变化显示（纯浏览/选择页统一外观；筛选仍按真实状态）
- 激活方案不再改动全局 mod 列表 / 分组 / 标签 / 备注，已读取 Mod 页始终展示磁盘默认配置
- 方案/集合下拉面板改为 portal 到 `document.body`，不再被 `overflow-hidden` 裁剪，且视口底部自动翻转、滚动/缩放跟随
- 方案名默认日期改点号分隔（如「方案 2026.9.1」），不再产生文件名非法字符
- 集合分组/顺序持久化到集合数据，切换视图或页面重挂载不再丢失

### 修复

- 多选拖入分组只移动一个：修复受控 `setGroups`/`setDisplayOrder` 陈旧函数式更新、及 `useCardDrag` 拖拽闭包选择状态过期（`multiDrag` 恒 false 且误清真实多选）
- 删除激活方案时自动清空激活状态
- 方案/集合编辑不再误置"未保存的更改"，关闭启动器不再弹出确认框（启动时若存在激活方案仍会按最新编辑同步写盘）
- 方案名含 `/`、`\` 等非法字符导致保存失败（如「方案 2026/9/1」）：统一过滤 + 弹窗提示
- 新增/修改/删除标签时下拉不再拦截点击而误关标签窗口（对话框与面板事件双层隔离）

## v1.9.4.0 — 配置修复更新

### 修复

- 配置页选项的显示名与描述现支持 Unity 富文本标签：`<color>` 颜色、`<b>` 加粗、`<i>` 斜体、`<size>` 字号（支持嵌套与跨行；未知/非法标签按字面显示）
- 配置页支持 `SettingType = "InputField"` 设置项的读取与修改（默认值为数字时按数字输入处理，空值失焦回退）
- 修复 Settings.Lua 字符串读写未转义/未解码的问题：含引号、反斜杠、换行的值现在可正确往返保存

## v1.9.3.0 — 拖拽与多选修复更新

### 修复

- Shift+点击无法触发范围多选（mousedown 过早清除 lastClickedKey）
- 分组内 Shift+点击范围多选错误选中全部分组外 Mod
- Ctrl+点击可跨上下文（分组内/外）混合多选，现已锁定上下文
- 拖拽分组内卡片到另一分组时蓝线与绿色高亮同时出现
- 分组内卡片拖拽到同组卡片上方时蓝线闪烁出现在非光标位置
- 分组内卡片拖到同组 header 上方时无法移出分组（top 40% 区域强制算在组内）
- 分组内拖拽排序蓝线不缩进，与分组外无区分
- 拖拽分组内卡片到分组末端时蓝线出现在分组顶端而非末端
- 分组内卡片拖到分组下方间隙时意外移出分组
- 多选单卡拖拽时 mousedown 不清除多选导致拖拽目标错误
- 分组创建拖拽未使用参数 `displayOrderRef` 导致 TS6133 编译错误

## v1.9.2.0 — 拖拽稳定性更新

### 修复

- 跨分组拖拽导致数据污染（displayOrder 未同步更新，分组归属与卡片位置不一致，影响"应用顺序"结果）
- 跨分组拖拽后空分组锚点指向错误位置，空分组头部跳到目标分组附近
- 未分组卡片拖到两个相邻分组之间时蓝色插入线不显示
- 分组内卡片拖出分组时经过其他分组头部，蓝色插入线消失
- 单卡/多卡拖拽松手后偶发位置偏移（事件闭包引用过时的 displayOrder）
- 分组重命名输入框偶发消失（点击交互元素时误清除编辑状态）

## v1.9.1.0 — 体验修复更新

### 修复

- 视口滚动失效（新增包裹 div 用 `display: contents` 恢复 flex 布局链）
- 快速点击开关/排序/目录等交互组件时误触发双击进入配置页
- Hook 顺序错误导致组件崩溃
- 分组拖拽蓝线在无操作位置仍显示

## v1.9.0.0 — 交互增强更新

### 新增

- **Mod 多选**：单击选中卡片，双击打开配置页；Ctrl+点击切换选中，Shift+点击范围选中；点击空白或按钮自动取消选中
- **多选拖拽**：拖拽已选中 Mod 中任意一张，全部选中项整体移动（支持归入/移出分组）
- **多选右键菜单**：选中多个 Mod 时右键显示批量操作 — 启用/禁用、发送到分组、上移/下移
- **右键打开配置页**：Mod 卡片右键菜单新增"打开配置页"选项（原"查看详情"）

### 改进

- 分组不再参与多选，点击分组仅清空 Mod 选中
- 选中内容变化时自动关闭右键菜单
- 点击按钮/输入框等交互元素时自动取消选中等状态

### 修复

- 分组拖拽重排序时无操作位置不再显示蓝线
- 分组卡片级拖拽时与相邻卡片的无操作蓝线不再显示

## v1.8.3.0 — 可靠性更新

### 新增

- **统一同步按钮**：Mod 开关、加载顺序、per-mod 配置的修改全部通过工具栏"同步"按钮一次性落盘，支持脏状态指示
- **方案保存空名兜底**：空名保存自动生成"新建方案"（已存在则递进编号）
- **分组重命名空名兜底**：空名或全空格自动使用"未命名分组"
- **方案导入版本校验**：拒绝高于当前版本的方案文件并提示不兼容
- **per-mod 配置写入验证**：写入后读回比对，确保内容完整性

### 改进

- **加载顺序输入**：负数/非数字/空值自动恢复原值，去前导零
- **打开 Mod 目录**：前置检测目录是否存在，不存在则弹窗提示
- **Steam 工坊跳转**：前置检测 Steam 是否安装，未安装则弹窗提示
- **右键"发送到分组"**：无分组时按钮置灰不可点击
- **刷新**：有未保存更改时弹确认框；路径失效时跳转欢迎页并提示
- **方案管理**：删除/覆盖/导入均增加确认对话框
- **方案/配置保存**：全部改为原子写入（临时文件 + rename），防止崩溃损坏文件
- **本地启动游戏**：PID 检测改为 5 次重试（共 10s），失败返回明确错误提示
- **Steam 启动游戏**：超时（~30s）弹出红色错误横幅"Steam 启动超时，游戏进程未检测到"
- **缓存路径失效提示**：启动时检测到缓存路径不可用时显示消息"缓存的游戏路径已失效"
- **部分 Mod 解析失败**：扫描时收集失败 Mod 名称并在消息栏提示
- **退出确认去重**：修复未同步退出时弹两次确认框的问题

### 修复

- 方案加载不再自动同步，改为设置脏状态提示用户手动点击同步
- Mod 配置页修改实时纳入全局脏状态，移除退出弹窗
- `window.destroy` 权限缺失导致退出报错（capabilities 增加 `core:window:allow-destroy`）
- 未分组卡片拖入已有成员的分组时，误执行位置移动而非归入分组
- 分组首位卡片向上拖出分组时，蓝色插入线缺失
- 拖拽卡片到紧邻下方卡片上半部分时，显示无操作蓝线（splice 调整后原位不动）
